# Optional. Only needed for the LLM multi-agent overlay (python ai_hedge_fund.py).
# The production systemd run (python main.py) uses public Kraken data and needs no keys.

AI_API_KEY=
AI_BASE_URL=https://chat-api.tamu.ai/api
AI_MODEL=protected.gemini-2.5-flash-lite
TAVILY_API_KEY=
